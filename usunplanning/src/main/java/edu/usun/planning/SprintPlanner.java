package edu.usun.planning;

public class SprintPlanner {
	
	/**
	 * @param args List of execution parameters.
	 */
	public static void main(String[] args) {
		/*
		SprintManager sprintManager = SprintManager.getInstance();
		// Empty sprints generation
		List<Sprint> sprints = sprintManager.prepFutureSprintsForPlanning();
		
		// Plan sprints according to the configured strategy
		IPlanningStrategy planningStrategy = PlanningStrategyManager.getInstance().getPlanningStrategy();
		planningStrategy.planSprints(sprints);
		
		// Generate sprints planning report
		IPlanningOutput planningOutput = PlanningOutputManager.getInstance().getPlanningOutput();
		planningOutput.writeSprintsPlan(sprints);
		*/
		System.out.println("Placeholder sprint planner");
	}
}
